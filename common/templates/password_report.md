# SSH Honeypot Password Report

**Password:** `{{ password }}`

## Usage Statistics

| Metric | Value |
|--------|-------|
| Total Attempts | **{{ total_attempts }}** |
| Unique IP Addresses | {{ unique_ips }} |
| Unique Usernames | {{ unique_usernames }} |
| First Seen | {{ first_seen | fmt("%Y-%m-%d %H:%M:%S UTC") }} |
| Last Seen | {{ last_seen | fmt("%Y-%m-%d %H:%M:%S UTC") }} |

## Top Usernames

| Rank | Username | Attempts |
|------|----------|----------|
{% for row in top_usernames %}| {{ row.rank }} | `{{ row.value }}` | {{ row.count }} |
{% endfor %}
## Top IP Addresses

| Rank | IP Address | Attempts |
|------|------------|----------|
{% for row in top_ips %}| {{ row.rank }} | `{{ row.value }}` | {{ row.count }} |
{% endfor %}
## All Usernames ({{ all_usernames | length }})

| Username | Attempts |
|----------|----------|
{% for row in all_usernames %}| `{{ row.value }}` | {{ row.count }} |
{% endfor %}
## All IP Addresses ({{ all_ips | length }})

| IP Address | Attempts |
|------------|----------|
{% for row in all_ips %}| `{{ row.value }}` | {{ row.count }} |
{% endfor %}

---
*Report generated by SSH Honeypot Report Generator*
